<%- include('partials/header') %> <%- include('partials/navbar') %>

<header class="jumbotron">
    <div class="container">
        <h1>Welcome to <a href="/">YelpCamp</a></h1>
        <h2>Add a new Campground</h2>
        <p>Create a new campground by adding a name, and image URL.</p>
        <a class="btn btn-primary btn-lg" href="/campgrounds"
            >View Campgrounds</a
        >
    </div>
</header>

<main class="new-campground">
    <div class="container">
        <form action="/campgrounds" method="POST">
            <div class="form-group">
                <label for="name-input">Campground Name</label>
                <input
                    class="form-control"
                    type="text"
                    name="name"
                    id="name-input"
                    placeholder="Undermountain Springs"
                />
            </div>
            <div class="form-group">
                <label for="desc-input">Campground Description</label>
                <input
                    class="form-control"
                    type="text"
                    name="desc"
                    id="desc-input"
                    placeholder="Trees, fish, secluded from society"
                />
            </div>
            <div class="form-group">
                <label for="image-input">Image URL</label>
                <input
                    class="form-control"
                    type="text"
                    name="image"
                    id="image-input"
                    placeholder="https://campgroundpics.com/"
                />
            </div>
            <input
                class="btn btn-block btn-lg btn-primary"
                type="submit"
                value="Add Campground"
            />
        </form>
    </div>
</main>

<%- include('partials/footer') %>
